import React from 'react'

const MainTitle = ({label}) => {
  return (
    <span className='text-mainColor w-max mx-auto block capitalize font-mono text-[20px] mb-10'>{label}</span>
  )
}

export default MainTitle